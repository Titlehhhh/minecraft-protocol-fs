@AGENTS.md

The imported AGENTS.md holds the full agent instructions (working loop, where protocol facts
come from, DSL cheat-sheet, build/codegen). Raw minecraft-data: canon — the workspace
AGENTS.md (forbidden always; a surface gap = stop and ask the owner). For Claude, any
exceptions are physically closed by the deny rules in `.claude\settings.json`.
