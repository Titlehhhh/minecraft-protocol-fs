@AGENTS.md

The imported AGENTS.md holds the full agent instructions (working loop, where protocol facts
come from, DSL cheat-sheet, build/codegen). Key rule: model types from McProtoFacts' prepared
facts (`scripts\facts.cmd` / `scripts\serve-facts.cmd`), never from raw minecraft-data json.
